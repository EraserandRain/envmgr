---
# Global vars file for ai_tools
volta_root: "{{ ansible_facts['env']['HOME'] }}/.volta"
ai_tools_user_bin_path: "{{ ansible_facts['env']['HOME'] }}/.local/bin"

# AI tools configuration
ai_tools_config:
  claude_code:
    enabled: true
    package: "@anthropic-ai/claude-code"
    binary: "claude"
    description: "Anthropic's Claude Code CLI"
  codex:
    enabled: true
    package: "@openai/codex"
    binary: "codex"
    description: "OpenAI's Codex CLI"
  rtk:
    enabled: true
    binary: "rtk"
    description: "RTK CLI proxy"
    install_dir: "{{ ai_tools_user_bin_path }}"
    latest_release_url: "https://github.com/rtk-ai/rtk/releases/latest"
    latest_download_base_url: "https://github.com/rtk-ai/rtk/releases/latest/download"
    release_download_base_url: "https://github.com/rtk-ai/rtk/releases/download"
