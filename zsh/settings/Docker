#!/usr/bin/env zsh

# Set Device
local Device="Docker $(hostname -s)"

# Loading
source $HOME/install/include/common.sh
set_git_config

load_env \
    --nvm \
    --alias \
    --pyenv \
    --ssh \
    --zsh $Device
